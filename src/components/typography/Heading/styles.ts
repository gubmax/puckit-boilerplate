import { css } from '@emotion/react'

const styles = {
  h1: css`
    margin-bottom: var(--space-secondary);
    font-size: var(--fs-h1);
    font-weight: bold;
  `,
  h2: css`
    margin-bottom: var(--space-secondary);
    font-size: var(--fs-h2);
  `,
}

export default styles
