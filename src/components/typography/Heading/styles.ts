import { css } from '@emotion/react'

const styles = {
  h1: css`
    margin-bottom: var(--space-secondary);
    font-size: var(--fs-h1);
    font-weight: bold;
  `,
  h2: css`
    margin-bottom: var(--space-secondary);
    font-size: var(--fs-h2);
    font-weight: bold;
  `,
  h3: css`
    font-size: var(--fs-h3);
  `,
}

export default styles
