import { css } from '@emotion/react'

const styles = {
  wrapper: css`
    display: flex;
    align-items: center;
  `,
  counter:  css`
    margin-left: var(--space-secondary);
    font-size: var(--fs-h2);
  `
}

export default styles
