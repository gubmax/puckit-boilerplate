import { css } from '@emotion/react'

const styles = {
  wrapper: css`
    grid-area: header;
    display: flex;
    align-items: center;
    padding: var(--space-primary);
  `,
  logoWrapper: css`
    margin-bottom: 0;
  `,
}

export default styles
