import { css } from "@emotion/react"

const styles = {
  wrapperStyles: css`
    max-width: 1024px;
    margin: 0 auto;
    padding: 0 16px;
  `,
  titleStyles: css`
    font-size: 20px;
  `
}

export default styles
