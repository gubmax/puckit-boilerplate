import { css } from '@emotion/react'

const styles = {
  wrapper: css`
    grid-area: aside;
    padding: 20px;
    background: var(--color-dark);
  `,
  title: css`
    font-size: 24px;
  `,
}

export default styles
